import { StyleSheet, Text, View } from "react-native";
import React from "react";

// this allow user to either select pic from camera roll / take picture
// as their profile image. -- should be saved to firebase storage pls write appropriate
// functions under Firebase folders
export default function ImageManager() {
  return (
    <View>
      <Text>ImageManager</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
